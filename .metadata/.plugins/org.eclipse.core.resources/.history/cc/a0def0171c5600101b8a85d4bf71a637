package ex03_강버들;

public class Ex03 {
	
	
	
	
	//메소드 정의
	
	//1) 기능 정의하기
	//정수 배열을 받아 최대값과 최소값을 구하는 메소드
	// getMixAndMax
	
	//2) 반환값 지정
	//정수 배열을 받아 최대값과 최소값을 구하는 메소드
	//void getMixAndMax
	
	//3) 매개변수 지정
	//정수 배열을 받아 최대값과 최소값을 구하는 메소드
	//void getMixAndMax(int[] arr)
	
	//4) 구현부 작성
	//정수 배열을 받아 최대값과 최소값을 구하는 메소드
	//void getMixAndMax(int[] arr){
		//4-1) 최대값과 최소값을 담을 정수형 변수 각 1개씩 선언, 초기화(max, min 총 2개)
		//4-2) 반복문 (배열의 크기 만큼 반복)
			//if(현재 인덱스의 정수가 max보다 큰 경우) max에 현재 값 대입;
			//if(현재 인덱스의 정수가 min보다 작은 경우) min에 현재 값 대입;
		// 반복문 끝
		//4-3) 최대값과 최소값 출력
	//}
	
	
}
